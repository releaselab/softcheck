(executables
  (names tipTest cubicTest)
  (modules tipTest cubicTest)
  (libraries kaputt softcheck tip)
  (flags -g)
  (modes byte))

(alias
  (name runtest)
  (deps ../tip/exe/main.exe)
  (action (progn (run ./tipTest.exe) (run ./cubicTest.exe))))

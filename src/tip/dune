(copy_files# analysis/*)
(copy_files# ast/*)
(copy_files# flow/*)
(copy_files# cfg/*)
(copy_files parser/*)

(library
  (name tip)   ; doesn't support '-' in the name
  (libraries softcheck analysis)
  (synopsis "Softcheck support for TIP."))

(ocamllex lexer)

(menhir
  (modules parser))
